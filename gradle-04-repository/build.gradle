apply plugin: 'java' // needle for the dependency scope (pre-configure on this plugin)

repositories {
	mavenCentral()
}
dependencies {
	compile 'com.google.code.gson:gson:2.6.2' // is only example this dependency is not used
//		classpath files("libs/some.library.jar") // example of how call local jars
}

task hello << {
	def personDetails = [firstName:'John', lastName:'Doe', age:25] // a hashmap example
	println personDetails
}